- common configuration
- separate 'mypipe.2.NormAlign.sh" as helper tool, since barely used and bam file can be handled

alignment file normalization

makeTagDirectory
makeTagDir.sh <option> tagDir aligFile

peakCalling
stack-height profile


